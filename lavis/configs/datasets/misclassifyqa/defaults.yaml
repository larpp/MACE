# lavis/config/datasets/misclassify/default.yaml
datasets:
  misclassifyqa:
    # data_dir: ${env.data_dir}/datasets
    data_type: images # [images|videos|features]

    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          storage: /home/MACE/input/misclassifyqa/train.csv
        # val:
          # storage: /input/misclassify/misclassify_val.json
        test:
          storage: /home/MACE/input/misclassifyqa/test.csv # 원래는 test.csv / test_data.csv
          # storage: /home/InstructBLIP_PEFT/answer.csv
      images:
        storage: /home/MACE/input
        train:
          storage: /home/MACE/input
        # val:
          # storage: /input
        test:
          storage: /home/MACE/input # 원래는 input / input_test
